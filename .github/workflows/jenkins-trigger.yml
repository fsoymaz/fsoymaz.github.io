name: Trigger Jenkins

on:
  push:
    branches:
      - test
      - main
  workflow_dispatch:

jobs:
  trigger-jenkins:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger Jenkins Webhook
        run: |
          echo "Jenkins pipeline should be triggered via webhook or polling"
          echo "Branch: ${{ github.ref_name }}"
          echo "This workflow is a placeholder. Configure Jenkins to poll or use webhook."

